# 前端开发环境 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装开发工具
RUN apk add --no-cache git

# 复制 package 文件
COPY package*.json ./

# 安装依赖（包括开发依赖）
RUN npm ci

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 设置环境变量
ENV NODE_ENV=development

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]